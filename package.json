{
  "name": "youtracker",
  "productName": "YouTracker",
  "version": "1.0.1",
  "description": "",
  "main": "build/main.bundle.js",
  "scripts": {
    "start": "webpack --mode=development && electron .",
    "build": "webpack --mode=production && electron-builder --publish never",
    "publish": "webpack --mode=production && electron-builder --mac --win --linux --publish always",
    "postinstall": "electron-builder install-app-deps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.4.4",
    "@babel/preset-env": "^7.4.4",
    "@babel/preset-react": "^7.0.0",
    "babel-loader": "^8.0.6",
    "css-loader": "^3.2.0",
    "electron": "^6.0.2",
    "electron-builder": "^21.2.0",
    "node-loader": "^0.6.0",
    "style-loader": "^1.0.0",
    "webpack": "^4.39.2",
    "webpack-cli": "^3.3.7"
  },
  "dependencies": {
    "electron-store": "^3.3.0",
    "electron-updater": "^4.1.2",
    "keytar": "^4.13.0",
    "node-fetch": "^2.6.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "build": {
    "appId": "ru.smarthead.YouTracker",
    "extraMetadata": {
      "main": "build/main.bundle.js"
    },
    "files": [
      "build/**/*",
      "static/**/*"
    ],
    "directories": {
      "buildResources": "assets"
    },
    "publish": {
      "provider": "s3",
      "bucket": "youtracker.smarthead.ru"
    },
    "mac": {
      "darkModeSupport": true,
      "target": "dmg",
      "artifactName": "${productName}.${ext}"
    },
    "win": {
      "target": "nsis",
      "artifactName": "${productName}-installer.${ext}"
    },
    "linux": {
      "target": "AppImage",
      "artifactName": "${productName}.${ext}"
    }
  }
}
